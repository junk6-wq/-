# 旅のしおり プロジェクト

## このリポジトリは何か

`index.html` 1ファイルで完結する旅程しおり。GitHub Pages で公開している。

- 公開URL: https://junk6-wq.github.io/-/
- 実体: **`/index.html`**（単一ファイル・約1,300行・CSS/JSインライン）
- 画像: `/images/`

**しおりを探しにNotionやGoogle Driveへ行かないこと。** Drive上には同名のドキュメントが
複数あるが、すべて旧版または検討中の別案であって、公開物ではない。
「公開しているしおり」と言われたら、それはこのリポジトリの `index.html` を指す。

## 使われ方

計画中だけでなく、**旅行中の車内・現地から書き換える**。
過去のコミットが示すとおり、「渋滞で予定が崩れた」「今この場所にいる」を受けて
再計算し、その場で反映してきた。

---

## 作業を始める前に必ずやること

### 1. 現在日時を実データで確定する

**相対表現（「明日」「今日」）をそのまま信用しない。**
ユーザーの言う「明日」とシステムの日付がずれていることがある。
旅程は時刻の積み上げで成立しているため、起点がずれると全部が無意味になる。

現在地の天気APIは日時と気象を同時に取れるので、時刻確認を兼ねられる:

```bash
curl -s "https://api.open-meteo.com/v1/forecast?latitude=36.3487&longitude=138.6329&current=temperature_2m,precipitation&timezone=Asia%2FTokyo" | head
```

### 2. ユーザーが今どこにいるかを確認する

自宅で計画しているのか、車の中か、目的地に立っているのかで出すべきものが変わる。

| 状況 | 出力 |
|---|---|
| 計画中（自宅） | 比較表・選択肢・根拠を厚く |
| 移動中・現地 | **結論 → 根拠2行 → 次の行動**。表は最大1つ |

---

## 助言の原則

### 提案で終わらせない — これが最重要

**助言が採用されたら `index.html` に反映してコミットするまでが完了。**

「書き込みますか？」と聞いて止まると、口頭のやり取りだけが残り、しおりが実態から
ずれていく。現地で見るのはしおりであって会話ログではない。
迷ったら、反映した上で「こう書きました」と報告する方がよい。

### 予報より実測・ライブカメラを優先する

「その景色が見えるか」は気象モデルでは決まらない。複数モデル（ECMWF/ICON/GFS）が
割れて結論が出せないとき、**カメラ1本で決着する**。

- 横手山山頂2307m: https://yokoteyama2307.com/live_camera/
- 志賀高原公式（横手山山頂）: http://www.shigakogen.gr.jp/live/live_camera_yokote.php
- 横手山頂ヒュッテ: http://www.yokoteyama.com/livecamera

雲海は「自分が雲の上に出るか」で決まる。気圧面の雲量（850hPa≒1,480m /
800hPa≒2,000m）を見て雲頂高度を推定し、目的地の標高と比較する。
なお雲海（放射霧型）は日射で急速に消えるため、**日の出前後のごく短時間**が勝負。
リフトの始発（横手山スカイレーター 8:45）では間に合わないことがある。

### 移動時間はしおりの実測値を使う

`index.html` には実走に基づく数値が書き込まれている
（例: 国道292号 志賀高原〜渋峠は平均35km/h、道の駅白馬➔万座 82km・実用2時間30分）。
**勝手に安全側へ盛らない。** 盛る場合は盛った旨と理由を書く。

### 候補は用途に縛らず全列挙する

しおりの中で既に別の役割を与えられている場所も、候補として検討する。

実例: 軽井沢の駐車場を調べたとき、プリンスショッピングプラザを「最後の買い物先」と
しか見ておらず**拠点候補から漏らした**。実際には約3,500台・2時間無料・以降100円/時で、
旧軽の有料駐車場（500〜1,700円）より安く満車リスクもない最良手だった。

### 一次情報でも日付の鮮度を見る

自治体ページには古い規制情報が残っていることがある。矛盾したら両方提示し、
確認先の電話番号を添える。

- 北信建設事務所（長野県側）0269-22-3139
- 中之条土木事務所（群馬県側）0279-75-3047

### 既成事実は覆さない

「もう精算した」「もう買った」「もう通過した」に対してやり直しを提案しない。
**それを前提にした最善手**を出す。

### 安全上の注記を省かない

以下は必ず一言添える。

- 早朝・夜間の山岳路走行
- 雨天の下り勾配（碓氷峠、志賀高原の急カーブ）
- 長距離ドライブの最終日の疲労

---

## 編集のルール

### 計画と実績を混ぜない

当日の更新は所定のalertで追記する（`class="alert warn"` に「⚠️ 8/12 当日更新：」等）。
確定した過去日は実績として畳み、これから使う日を前に出す。

### 時刻は積み上げで検算する

出発時刻＋移動時間＝到着時刻 が全区間でつながっているか通しで確認する。
過去に5件の齟齬が見つかっている（`b98d800`）。単一箇所の変更が後続を全部ずらす。

### タブの重複に注意

DAY2/3/4のタブが重複して壊れた履歴がある（`4ba64c1`）。
編集後は `id="dayN"` の重複と `switchTab()` の対応を確認する。

---

## ブランチ

作業ブランチ: `claude/tomorrow-schedule-polish-8b3tex`

`main` に直接コミットしない。PRはユーザーが明示的に求めたときだけ作る。
